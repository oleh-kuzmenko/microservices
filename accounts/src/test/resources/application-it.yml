spring:

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

cards:
  url: http://localhost:${wiremock.server.port}

loans:
  url: http://localhost:${wiremock.server.port}
